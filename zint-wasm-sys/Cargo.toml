[package]
name = "zint-wasm-sys"
version = "0.1.0"
edition = "2021"
description = "Rust bindings for Wasm build of zint"
license = "MIT"
categories = ["external-ffi-bindings"]

[build-dependencies]
cc = "1.0"
bindgen = "0.70.1"
walkdir = "2"
anyhow = "1"

[dev-dependencies]

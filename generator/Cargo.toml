[package]
name = "generator"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
zint-rs = { path = "../zint-wasm-rs", default-features = false, features = ["_gen_symbology_summary"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
